export enum ScreenWidth {
    MOBILE, SM, MD, LG, XL, XXL,
}

/** 
 * This variable is used at the home page for the "Dylan Alvandi" name. 
 * 
 * It sets the screen size limit which coincides with the ScreenWidth enum. The limit determines at which
 * size the animation of the home page name splits in a horizontal or vertical direction for
 * responsiveness.
 * **/
export const HOME_PAGE_NAME_DIRECTION_LIMIT = 2;